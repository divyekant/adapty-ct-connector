# Binaries (anchored to repo root — bare patterns would also ignore
# the cmd/<name>/ source directories)
/connector
/backfill
/mock-clevertap
bin/
*.exe

# Go
vendor/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Docker
docker-compose.override.yml

# Deploy state (per-tenant runtime IDs)
scripts/deploy/.state/
